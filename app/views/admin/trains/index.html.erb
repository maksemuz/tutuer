<h1><%=t('activerecord.models.trains') %></h1>

<table class="table table-striped" >
  <thead>
    <tr>
      <th><%=t('admin.trains.index.number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('admin.trains.new.number'), new_admin_train_path %>
<div></div>
<%= link_to t('admin.panels.index'), admin_panels_path %>